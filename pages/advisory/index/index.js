const app = getApp();
Page({
    data: {
        StatusBar: app.globalData.StatusBar,
        CustomBar: app.globalData.CustomBar,
        tab1: "新闻",
        tab2: "事件",
        tab3: "采访"
    }
});
